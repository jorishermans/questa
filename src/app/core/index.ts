import { NgModule } from '@angular/core';
import { AppTransferStateService } from './services/app-transfer-state.service';

@NgModule({
  providers: [AppTransferStateService]
})
export class CoreAppModule { }
